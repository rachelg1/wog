pipeline {
    agent any
    stages {
        stage('checkout') {
            steps {

            }
        }

        stage('build') {
            steps {

            }
        }

        stage('run') {
            steps {

            }
        }

        stage('test') {
            steps {

            }
        }

        stage('finalize') {
            steps {

            }
        }
    }
}
